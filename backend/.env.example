# ─────────────────────────────────────────────
# Airport Ride Pooling — Environment Variables
# ─────────────────────────────────────────────
# Copy this file to .env and fill in your values:
#   cp .env.example .env

# PostgreSQL connection string
DATABASE_URL="postgresql://postgres:YOUR_PASSWORD@localhost:5432/airport_pooling?schema=public"

# Server
PORT=3000
NODE_ENV=development

# Pricing (optional — defaults are set in src/config/index.js)
# BASE_FARE=50
# DISTANCE_RATE=12
# MAX_DEMAND_MULTIPLIER=2.5
# POOL_DISCOUNT_PERCENT=25
# DETOUR_PENALTY_PER_KM=5
